*{{.Spec.ModuleName}}.txt*   Scaffolded with plug-scaffold (style: omar)

Author:  {{.Spec.Author}}
License: MIT

==============================================================================
CONTENTS                                          *{{.Spec.ModuleName}}-contents*

  1. Introduction ................ |{{.Spec.ModuleName}}-intro|
  2. Setup ....................... |{{.Spec.ModuleName}}-setup|
  3. API ......................... |{{.Spec.ModuleName}}-api|
  4. Health ...................... |{{.Spec.ModuleName}}-health|

==============================================================================
1. Introduction                                      *{{.Spec.ModuleName}}-intro*

TODO: describe the plugin.

==============================================================================
2. Setup                                             *{{.Spec.ModuleName}}-setup*

setup() is optional — the plugin loads its `plugin/` entry on startup
and defers the rest to first `require()`. Call setup only when you
need to override defaults or inject dependencies.
>lua
    require("{{.Spec.ModuleName}}").setup({
      -- your config here
    })
<
The second argument accepts injected dependencies so tests can swap
production collaborators for fakes:
>lua
    require("{{.Spec.ModuleName}}").setup({}, { notifier = fake })
<

==============================================================================
3. API                                                 *{{.Spec.ModuleName}}-api*

Submodules:

- `require("{{.Spec.ModuleName}}").config`   — defaults and merge helper
- `require("{{.Spec.ModuleName}}").detector` — should_load() gate
- `require("{{.Spec.ModuleName}}").health`   — |:checkhealth| entry point

==============================================================================
4. Health                                           *{{.Spec.ModuleName}}-health*

Run `:checkhealth {{.Spec.ModuleName}}` to verify the plugin loaded and
the runtime environment is supported.

 vim:tw=78:ts=8:noet:ft=help:norl:
